# Daily Bite data

The iPhone shortcut creates one JSON file per shared note in this folder. Both vocabulary websites read these files automatically.

Required fields: `id`, `date`, `title`, and `content`. Optional fields: `source`, `sourceUrl`, `reflection`, `vocabulary`, and `createdAt`.

Do not store the GitHub access token in this repository. Keep it only in the iPhone shortcut.
