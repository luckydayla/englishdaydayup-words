# Processed vocabulary

Validated vocabulary sets generated from `word-intake/` are stored here. Both websites read these JSON files directly. Intake IDs are immutable, so retrying the workflow cannot publish the same record twice.
